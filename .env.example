# 复制成 .env 并填入真实值（.env 已在 .gitignore 中，不会被提交）
# 本地运行：  set -a && source .env && set +a && python main.py

# PushPlus 推送 token（必填）
PUSHPLUS_TOKEN=26614f5b8a874aab9ad4791555079520

# 群组编码（一对多推送），支持多个，逗号分隔
# 只发给自己就留空
PUSHPLUS_TOPICS=oai.1
# PUSHPLUS_TOPICS=oai.1,another-group

# 萝卜投研 Cloud-Sso-Token（选填）。
# 不填则该源自动降级为东财"机构盈利预测"，推送里会如实标注。
# 获取方式：登录 robo.datayes.com -> 开发者工具 -> 任一 gw.datayes.com 请求
#           的请求头 Cloud-Sso-Token
DATAYES_TOKEN=

# DeepSeek 大模型 API Key（选填）
# 用于一对一手动主题分析推送时自动调用 DeepSeek API 进行内容提炼、分类与摘要，再组合推送到微信。
# 默认模型 deepseek-v4-flash（OpenAI 兼容接口 https://api.deepseek.com）。
# 旧的 ZHIPU_API_KEY 仍会被回落读取，兼容未改名的历史配置。
DEEPSEEK_API_KEY=
# DEEPSEEK_MODEL=deepseek-v4-flash

# --- 主题因子分析（一对一）：python main.py --theme "人形机器人" -------------
# GitHub Token（选填）：拉取 microsoft/qlib 的 Alpha158 因子定义时使用。
# qlib 是公共仓库，不填也能读（未认证 60 次/小时），填了只是提高 API 限额。
GITHUB_TOKEN=
# 取板块内成交额前几只个股（默认 6）
# OCTOPUS_FACTOR_STOCK_TOP=6
# 取多少根日线算因子（默认 250）
# OCTOPUS_FACTOR_KLINE_LIMIT=250
# 监管动态回溯天数（默认 30）
# OCTOPUS_SUPERVISION_DAYS=30

# 主题因子分析的行情数据源：auto（默认，东财优先自动降级）/ eastmoney / yahoo
# 国外机器（海外 VPS、GitHub Actions）建议显式指定 yahoo —— 免费免注册无 Key
# OCTOPUS_FACTOR_MARKET_SOURCE=auto

# 覆盖配置（选填）
# OCTOPUS_WINDOW_MINUTES=180
# OCTOPUS_PUSH_WHEN_EMPTY=true
# OCTOPUS_DISABLED_SOURCES=datayes,hibor
# 夜间免打扰（北京时间），时段内暂停抓取与推送
# OCTOPUS_QUIET_START=23:00
# OCTOPUS_QUIET_END=07:00
